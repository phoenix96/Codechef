#include<iostream>
#include<stdio.h>
using namespace std;
int main()
{
    long long int in,ma=0,g=0,s=0,k=0;
    scanf("%lld",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&h[i]);
        if(h[i]>h[0])
        {
            ma=h[i];
        }
    }
    for(int i=0;i<n;i++)
    {
        scanf("%d",&cost[i]);
        m=m+h[i]*cost[i];
        sum=sum+
    }
    for(int i=0;i<ma;i++)
    {

    }
    return 0;
}
